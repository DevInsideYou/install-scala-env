# Sets up a [Scala](https://www.scala-lang.org/) dev environment on [Ubuntu](https://www.ubuntu.com/)

```bash
curl -fLOs https://script.install.devinsideyou.com/scala-env
chmod +x scala-env && ./scala-env
```
